#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""Rewrite AI-sounding numbered lists to natural prose in 初稿.md"""

import sys
sys.stdout.reconfigure(encoding='utf-8')

with open('初稿.md', 'r', encoding='utf-8') as f:
    content = f.read()

results = []

def do_replace(old_str, new_str, name):
    global content
    if old_str in content:
        content = content.replace(old_str, new_str)
        results.append(f"OK: {name}")
        return True
    else:
        results.append(f"NOT FOUND: {name}")
        return False

# ===== 2.4.2 + 2.4.3 =====
NL = "\n"
SP = " "
IMG12 = "![img](file:///C:\\Users\\Lenovo\\AppData\\Local\\Temp\\ksohtml34072\\wps12.jpg)"
IMG13 = "![img](file:///C:\\Users\\Lenovo\\AppData\\Local\\Temp\\ksohtml34072\\wps13.jpg)"
IMG14 = "![img](file:///C:\\Users\\Lenovo\\AppData\\Local\\Temp\\ksohtml34072\\wps14.jpg)"

old = (
    "### 2.4.2 情绪量化指标体系" + NL + NL
    "（1）看涨指数（Bull Index）。定义为积极评论数占总评论数的百分比，反映市场看多情绪的强度。计算公式为：" + NL + NL
    " " + NL + NL
    + IMG12 + " " + NL + NL
    "（2）看跌指数（Bear Index）。定义为消极评论数占总评论数的百分比，反映市场看空情绪的强度。计算公式为：" + NL + NL
    " " + NL + NL
    + IMG13 + " " + NL + NL
    " " + NL + NL
    "（3）情绪强度（Intensity）。定义为积极评论数与消极评论数之差占总评论数的比值，取值范围为[-1, 1]，反映市场整体的情绪偏向。计算公式为：" + NL + NL
    " " + NL + NL
    + IMG14 + NL + NL
    "（4）情绪温度（Temperature）。基于置信度加权的情绪综合分数，取值范围为[0, 100]，反映市场的综合热度。积极评论映射为分数1.0，消极评论映射为0.0，中性评论映射为0.5，以各自的置信度作为权重进行加权平均。" + NL + NL
    "（5）情绪波动（Volatility）。定义为一段时间窗口内情绪强度的标准差，反映市场情绪的稳定性。波动越大说明市场分歧越严重。" + NL + SP + NL + NL
    "### 2.4.3 情绪与市场走势的关联验证" + NL + NL
    "为验证情绪指标的有效性，本系统采用多种统计方法进行市场验证：" + NL + NL
    "（1）Pearson相关系数。衡量情绪强度与股价涨跌幅之间的线性相关程度。" + NL + NL
    "（2）Spearman秩相关系数。衡量情绪强度与股价涨跌幅之间的单调关系，对异常值更鲁棒。" + NL + NL
    "（3）方向预测准确率。统计情绪方向（正/负）与次日股价涨跌方向一致的比例。" + NL + NL
    "（4）格兰杰因果检验（Granger Causality Test）。检验情绪时间序列是否包含有助于预测股价变动的信息。若情绪是股价的格兰杰原因，则说明情绪指标具有领先预测价值[19]。"
)
new = (
    "### 2.4.2 情绪量化指标体系" + NL + NL
    "基于上述理论框架，本系统设计了五个相互补充的情绪量化指标。看涨指数（Bull Index）衡量积极评论在总体评论中的占比，反映市场中看多情绪的强度，计算公式为：" + NL + NL
    + IMG12 + NL + NL
    "看跌指数（Bear Index）与看涨指数相对应，以消极评论数为分子计算百分比，刻画市场中的看空情绪强度：" + NL + NL
    + IMG13 + NL + NL
    "情绪强度（Intensity）则综合考量了积极和消极评论的对比，用两者之差除以总评论数，取值范围为[-1, 1]，正值表示市场整体偏乐观，负值表示偏悲观：" + NL + NL
    + IMG14 + NL + NL
    "情绪温度（Temperature）采用置信度加权方案：对每条评论的情感标签赋予数值分数（积极=1.0，中性=0.5，消极=0.0），以各自的置信度作为权重进行加权平均，最后归一化到[0, 100]区间。相比简单计数，这一指标融入了模型判断的确定性程度——置信度高的评论对温度的影响更大。" + NL + NL
    "情绪波动（Volatility）计算一段时间窗口内情绪强度的标准差，反映市场情绪的稳定性。波动越大说明投资者之间的分歧越严重，情绪尚未形成一致预期。" + NL + NL
    "### 2.4.3 情绪与市场走势的关联验证" + NL + NL
    "为验证上述指标的有效性，本系统采用四种统计方法进行市场验证。Pearson相关系数衡量情绪强度与股价涨跌幅之间的线性相关程度，能够捕捉两者同向或反向变动的总体趋势。Spearman秩相关系数则衡量两者之间的单调关系，由于基于秩次计算而非原始数值，它对异常值具有更强的鲁棒性。方向预测准确率统计情绪方向（正向或负向）与次日股价涨跌方向一致的比例，这一指标直观反映了情绪信号在实际交易中的参考价值。格兰杰因果检验（Granger Causality Test）进一步判断情绪时间序列中是否包含有助于预测股价变动的信息——如果检验结果表明情绪是股价变动的格兰杰原因，则说明情绪指标对市场具有领先预测价值[19]。"
)
do_replace(old, new, "2.4.2+2.4.3")

# ===== 3.1.2 爬虫架构设计 =====
old = (
    "### 3.1.2 爬虫架构设计" + NL + NL
    "系统采用面向对象的爬虫架构，定义了BaseCrawler抽象基类，各平台爬虫通过继承基类实现具体的数据解析逻辑。核心设计如下：" + NL + NL
    "（1）抽象基类（BaseCrawler）。定义了爬虫的基本属性和方法，包括HTTP会话管理、请求发送、随机延迟、重试机制和状态管理。所有派生爬虫必须实现`crawl_stock`抽象方法。" + NL + NL
    "（2）东方财富爬虫（EastMoneyCrawler）。针对东方财富股吧的HTML页面结构实现数据解析，通过BeautifulSoup库提取帖子标题、作者、发布时间、阅读数和回复数等信息。支持分页爬取，默认限制最大5页。" + NL + NL
    "（3）爬虫管理器（CrawlerManager）。负责统一调度各平台爬虫，管理爬虫的启动、停止和状态查询。" + NL + NL
    "（4）数据项定义（CommentItem）。使用Python dataclass定义标准化的评论数据结构，包含评论ID、平台来源、股票代码、评论内容、作者、发布时间、点赞数和回复数等字段。"
)
new = (
    "### 3.1.2 爬虫架构设计" + NL + NL
    "系统采用面向对象的爬虫架构，定义了BaseCrawler抽象基类，各平台爬虫通过继承基类实现具体的数据解析逻辑。抽象基类定义了爬虫的基本属性和方法，包括HTTP会话管理、请求发送、随机延迟、重试机制和状态管理，所有派生爬虫必须实现`crawl_stock`抽象方法。东方财富爬虫（EastMoneyCrawler）继承该基类，针对东方财富股吧的HTML页面结构实现具体的数据解析逻辑，通过BeautifulSoup库提取帖子标题、作者、发布时间、阅读数和回复数等信息，支持分页爬取，默认限制最大5页。爬虫管理器（CrawlerManager）则负责统一调度各平台爬虫，管理爬虫的启动、停止和状态查询，提供一致的操控接口。数据项定义（CommentItem）使用Python dataclass描述标准化的评论数据结构，包含评论ID、平台来源、股票代码、评论内容、作者、发布时间、点赞数和回复数等字段，确保不同爬虫采集的数据格式统一。"
)
do_replace(old, new, "3.1.2 爬虫架构")

# ===== 3.2.1 文本清洗流程 =====
old = (
    "金融社区评论文本通常包含大量噪声信息，如HTML标签、URL链接、表情符号等，需要经过系统化的清洗处理。本系统的文本清洗采用流水线（Pipeline）模式，按以下顺序依次处理：" + NL + NL
    "第一步，清除HTML标签。使用正则表达式`<[^>]+>`匹配并移除所有HTML标签。" + NL + NL
    "第二步，清除URL链接。使用正则表达式匹配并移除`http://`和`https://`开头的URL。" + NL + NL
    "第三步，处理表情符号。移除`[xxx]`格式的文字表情。" + NL + NL
    "第四步，清除@用户。移除`@用户名`格式的引用。" + NL + NL
    "第五步，清除话题标签。移除`#话题#`格式的标签。" + NL + NL
    "第六步，清除特殊字符。保留中英文字符、数字和常用标点符号，移除其余特殊字符。" + NL + NL
    "第七步，规范"
    "化空白。将连续的空白字符合并为单个空格，并去除首尾空白。" + NL + NL
    "经过上述清洗流程，原始评论文本被转换为干净、规范的文本，为后续的分词和情感分析奠定基础。"
)
new = (
    "金融社区评论文本通常包含大量噪声信息，如HTML标签、URL链接、表情符号等，需要经过系统化的清洗处理。本系统的文本清洗采用流水线（Pipeline）模式，按以下顺序依次处理：首先使用正则表达式`<[^>]+>`匹配并移除所有HTML标签；接着清除以`http://`和`https://`开头的URL链接；然后处理并移除`[xxx]`格式的文字表情；再清除`@用户名`格式的用户引用和`#话题#`格式的话题标签；随后清除除中英文字符、数字和常用标点符号以外的所有特殊字符；最后将连续的空白字符合并为单个空格并去除首尾空白。经过这七个环节的流水线处理，原始评论文本被转换为干净、规范的文本，为后续的分词和情感分析奠定了良好基础。"
)
do_replace(old, new, "3.2.1 文本清洗")

# ===== 3.2.2 中文分词 =====
old = (
    "中文文本与英文不同，词与词之间没有天然的空格分隔，因此需要进行分词处理。本系统采用jieba分词库实现中文分词，并针对金融领域进行了定制优化：" + NL + NL
    "（1）自定义词典。预加载了130余个金融领域专业术语，如"涨停板""跌停板""融资融券""北向资金""科创板"等，确保这些复合词不会被错误切分。" + NL + NL
    "（2）停用词过滤。构建了包含常用停用词和金融无关词的停用词表（约200个），在分词后过滤掉"的""了""是"等无实际意义的虚词。" + NL + NL
    "（3）关键词提取。基于TF-IDF算法从文本中提取Top-K关键词，用于后续的方面级情感分析和热点词云展示。" + NL + NL
    "（4）股票代码识别。通过正则表达式从文本中识别6位数字格式的股票代码或`$XX123456$`格式的标准代码。"
)
new = (
    "中文文本与英文不同，词与词之间没有天然的空格分隔，因此需要进行分词处理。本系统采用jieba分词库实现中文分词，并针对金融领域进行了多项定制优化。其一是自定义词典的加载，预置了130余个金融领域专业术语，如"涨停板""跌停板""融资融券""北向资金""科创板"等，确保这些复合词不会被错误切分。其二是停用词过滤，构建了包含常用停用词和金融无关词的停用词表（约200个），在分词后过滤掉"的""了""是"等无实际意义的虚词，以减少分析噪声。其三是关键词提取，基于TF-IDF算法从文本中提取Top-K关键词，用于后续的方面级情感分析和热点词云展示。其四是股票代码识别，通过正则表达式从文本中识别6位数字格式或`$XX123456$`格式的股票代码，便于关联评论与具体标的。"
)
do_replace(old, new, "3.2.2 中文分词")

# ===== 3.2.3 文本去重 =====
old = (
    "社交媒体平台上常存在大量重复或近似重复的评论（如广告、灌水），这些噪声数据会影响情感分析的准确性。本系统采用两种去重策略" + NL + NL
    "（1）精确去重。对清洗后的文本进行分词、排序，然后计算MD5哈希值。相同哈希值的文本被判定为完全重复并过滤。" + NL + NL
    "（2）模糊去重。对于非完全相同但高度相似的文本，使用Jaccard相似度进行判断。计算两个文本分词集合的交集与并集之比，当相似度超过0.8时判定为近似重复。"
)
new = (
    "社交媒体平台上常存在大量重复或近似重复的评论，如广告、灌水等，这些噪声数据会影响情感分析的准确性。本系统采用两级去重策略来处理这一问题。精确去重对清洗后的文本进行分词、排序后计算MD5哈希值，相同哈希值的文本被判定为完全重复并直接过滤。模糊去重则针对非完全相同但高度相似的文本，使用Jaccard相似度进行判断——计算两个文本分词集合的交集与并集之比，当相似度超过0.8时判定为近似重复并过滤。这种组合策略既能消除明显的重复内容，又能在一定程度上过滤掉改写后的近似重复评论。"
)
do_replace(old, new, "3.2.3 文本去重")

# ===== 3.3.2 数据表结构设计 =====
old = (
    "系统共设计了8张核心数据表，其中最重要的包括：" + NL + NL
    "（1）stocks表（股票信息表）。存储股票基本信息，以stock_code（股票代码）为主键，包含股票名称、所属行业、上市市场、上市日期和状态等字段。在industry和market字段上建立索引以加速查询。" + NL + NL
    "（2）comments表（评论数据表）。存储爬取的投资者评论，以自增ID为主键，comment_id为唯一业务键。核心字段包括平台来源、股票代码（外键关联stocks表）、评论原文、清洗后文本、作者、发布时间、点赞数和回复数。在stock_code、platform、publish_time和is_processed字段上建立索引。" + NL + NL
    "（3）sentiments表（情感分析结果表）。 存储每条评论的情感分析结果，与comments表通过comment_id一对一关联。核心字段包括情感标签（positive/neutral/negative）、置信度、三分类的概率得分和模型版本。" + NL + NL
    "（4）emotions表（情绪指标表）。** 存储按股票和日期/小时聚合的情绪量化指标。核心字段包括股票代码、统计日期、统计小时（NULL表示日级别）、各类评论计数、看涨指数、看跌指数、情绪强度、情绪温度和情绪波动。在(stock_code, stat_date, stat_hour)上建立唯一索引。" + NL + NL
    "（5）quotes表（行情数据表）。存储股票日线行情，核心字段包括股票代码、交易日期、OHLC价格、涨跌幅、成交量和换手率。在(stock_code, trade_date)上建立唯一索引。" + NL + NL
    "（6）experiment_results表（实验结果表）。存储多模型对比实验的结果，包括模型名称、数据集、准确率、F1分数、混淆矩阵等。" + NL + NL
    "（7）aspect_sentiments表（方面级情感表）。存储方面级情感分析结果，包括评论ID、方面类别（业绩/政策/技术/资金/行业）、情感标签和匹配关键词。" + NL + NL
    "（8）alerts表（预警记录表）。 存储情绪预警触发记录，包括股票代码、预警类型、严重程度、触发指标值、阈值和已读状态。"
)
new = (
    "系统共设计了8张核心数据表，共同支撑从原始评论到量化指标的全链路数据存储。stocks表存储股票基本信息，以stock_code为主键，包含股票名称、所属行业、上市市场、上市日期和状态等字段，并在industry和market字段上建立索引以加速查询。comments表存储爬取的投资者评论，以自增ID为主键、comment_id为唯一业务键，核心字段包括平台来源、股票代码（外键关联stocks表）、评论原文、清洗后文本、作者、发布时间、点赞数和回复数，并在stock_code、platform、publish_time和is_processed等字段上建立索引以优化查询性能。sentiments表存储每条评论的情感分析结果，通过comment_id与comments表一对一关联，核心字段包括情感标签（positive/neutral/negative）、置信度、三分类概率得分和模型版本。emotions表存储按股票和日期/小时聚合的情绪量化指标，包含各类评论计数、看涨指数、看跌指数、情绪强度、情绪温度和情绪波动，在(stock_code, stat_date, stat_hour)上建立唯一索引以支持增量更新。quotes表存储股票日线行情，核心字段包括OHLC价格、涨跌幅、成交量和换手率，在(stock_code, trade_date)上建立唯一索引。experiment_results表存储多模型对比实验的结果，包括各模型的准确率、F1分数和混淆矩阵等。aspect_sentiments表存储方面级情感分析结果，标注每条评论在业绩、政策、技术面、资金、行业五个维度上的情感标签和匹配关键词。alerts表存储情绪预警触发记录，标注预警类型、严重程度（high/medium/low三档）、触发时的指标值和阈值，以及已读状态。"
)
do_replace(old, new, "3.3.2 数据表")

# ===== 3.3.3 缓存策略 =====
old = (
    "为降低数据库访问压力，系统实现了两级缓存架构：" + NL + NL
    "（1）Redis主缓存。采用TTL（Time-To-Live）过期策略，通过连接池管理Redis连接。缓存键采用分层命名规范，如`stock:{code}:emotion`、`sentiment:stats`、`crawler:status`。" + NL + NL
    "（2）内存LRU备缓存。当Redis不可用时自动降级到内存缓存。采用LRU（Least Recently Used）淘汰策略，最大容量1000条。使用Python的OrderedDict实现，每次访问时将键移到末尾，容量满时淘汰最早（头部）的键。"
)
new = (
    "为降低数据库访问压力，系统实现了两级缓存架构。Redis主缓存采用TTL过期策略，通过连接池管理Redis连接，缓存键采用分层命名规范，如`stock:{code}:emotion`、`sentiment:stats`、`crawler:status`，确保缓存键名的可读性和可管理性。当Redis不可用时，系统自动降级到内存LRU备缓存，最大容量1000条，使用Python的OrderedDict实现——每次访问时将键移到末尾，容量满时淘汰最早（头部）的键。这种两级架构在保证性能的同时也提供了完善的容错能力。"
)
do_replace(old, new, "3.3.3 缓存策略")

# ===== 3.4.1 前端技术栈 =====
old = (
    "系统前端采用Vue 3框架构建单页应用（SPA），主要技术栈包括：" + NL + NL
    "（1）Vue 3 + Composition API。采用组合式API实现组件逻辑复用和更清晰的代码组织。" + NL + NL
    "（2）Vue Router。实现前端路由管理和导航守卫（登录验证）。" + NL + NL
    "（3）Pinia。作为状态管理方案，存储用户认证信息等全局状态。" + NL + NL
    "（4）Element Plus。基于Vue 3的UI组件库，提供表格、表单、卡片、弹窗等丰富的界面组件。" + NL + NL
    "（5）ECharts + vue-echarts。专业级数据可视化库，用于绑定绘制折线图、柱状图、饼图、雷达图等图表。" + NL + NL
    "（6）Axios。HTTP请求库，封装了请求/响应拦截器，统一处理JWT认证和错误提示。" + NL + NL
    "（7）Vite。新一代前端构建工具，提供极速的开发服务器热更新和高效的生产环境打包。"
)
new = (
    "系统前端采用Vue 3框架构建单页应用（SPA），技术栈的选择综合考虑了开发效率和运行性能。Vue 3搭配Composition API实现组件逻辑的复用和清晰组织；Vue Router负责前端路由管理和导航守卫，确保未登录用户无法访问受保护页面；Pinia作为全局状态管理方案，集中存储用户认证信息等跨组件共享的状态；Element Plus提供了表格、表单、卡片、弹窗等丰富的UI组件，大幅加快了界面开发速度；ECharts配合vue-echarts用于绘制折线图、柱状图、饼图、雷达图等各类数据可视化图表；Axios封装了请求和响应拦截器，统一处理JWT认证令牌和错误提示逻辑；Vite作为新一代构建工具，为开发阶段提供了极速的热更新体验，同时在生产环境实现了高效的打包压缩。"
)
do_replace(old, new, "3.4.1 前端技术栈")

# ===== 3.4.2 核心可视化页面 =====
old = (
    "系统共包含11个前端页面，核心可视化功能包括：" + NL + NL
    "（1）情绪仪表盘（Dashboard）。系统首页，顶部展示四张统计卡片（看涨指数、看跌指数、情绪温度、评论总量），中部展示情绪趋势折线图和行业情绪排行榜，底部集成预警通知栏，当有未读预警时以Alert组件形式显示。" + NL + NL
    "（2）股票列表（Stocks）。 支持按行业、市场筛选的股票列表页，以表格形式展示股票代码、名称、所属行业和最新情绪指标，支持搜索和分页。" + NL + NL
    "（3）股票详情（StockDetail）。个股深度分析页面，展示该股票的情绪趋势与行情走势叠加图、方面级情感分析雷达图（5个维度：业绩、政策、技术面，资金流向、行业趋势）、近期评论列表及其情感标注。" + NL + NL
    "（4）情绪趋势（Trend）。支持自定义时间范围的情绪趋势分析页面，可选择单只或多只股票进行情绪对比，展示看涨指数、看跌指数、情绪温度等指标的时序变化。" + NL + NL
    "（5）市场验证（Validation）。展示情绪指标与市场走势的相关性分析报告，包括Pearson/Spearman相关系数表格、方向预测准确率统计、最优领先天数分析。" + NL + NL
    "（6）情感分析器（Sentiment）。提供在线文本情感分析测试功能，用户输入任意文本即可实时获取情感分析结果和置信度。" + NL + NL
    "（7）预警管理（Alerts）。展示系统触发的情绪预警记录，支持按严重程度筛选和标记已读。"
)
new = (
    "系统共包含11个前端页面，覆盖了从市场概览到个股深度的完整分析链条。情绪仪表盘（Dashboard）作为系统首页，顶部以四张统计卡片展示看涨指数、看跌指数、情绪温度和评论总量等核心指标，中部呈现情绪趋势折线图和行业情绪排行榜，底部则集成预警通知栏，有未读预警时以醒目形式提示。股票列表（Stocks）支持按行业和市场条件筛选，以表格形式展示股票代码、名称、行业和最新情绪指标，并提供搜索和分页功能。股票详情（StockDetail）面向单只股票的深度分析，呈现情绪趋势与行情走势的叠加对比图、五个维度的方面级情感雷达图，以及近期评论列表及其情感标注。情绪趋势（Trend）页面允许用户自定义时间范围，选择单只或多只股票进行情绪指标的多维度对比分析。市场验证（Validation）页面展示相关性分析报告，包含Pearson/Spearman相关系数表格、方向预测准确率统计和最优领先天数分析等关键内容。情感分析器（Sentiment）提供在线文本测试功能，用户输入任意文本即可实时获得情感分类结果和置信度。预警管理（Alerts）页面集中展示系统触发的情绪预警记录，支持按严重程度筛选和一键标记已读。"
)
do_replace(old, new, "3.4.2 核心页面")

# ===== 4.1.2 核心数据流 =====
old = (
    "系统的核心数据流遵循"\"采集→处理→分析→量化→验证→展示\""的六阶段流水线：" + NL + NL
    "第一阶段，数据采集。爬虫服务从东方财富股吧采集投资者评论，解析HTML页面提取评论内容、作者和发布时间等信息，存入comments表。" + NL + NL
    "第二阶段，文本处理。文本处理器对原始评论进行清洗（去除HTML标签、URL、表情等噪声）、分词（基于jieba的金融定制化分词）和去重（MD5哈希精确去重+Jaccard模糊去重），清洗后文本写入content_clean字段。" + NL + NL
    "第三阶段，情感分析。BERT模型对清洗后文本进行三分类推理，输出情感标签（积极/中性/消极）和置信度，结果存入sentiments表。同时，方面级分析器对文本进行业绩、政策、技术面，资金流向、行业趋势五个维度的情感判断，结果存入aspect_sentiments表。" + NL + NL
    "第四阶段，情绪量化。情绪计算器按股票和时间粒度（日级/小时级）聚合情感分析结果，计算看涨指数、看跌指数、情绪强度、情绪温度和情绪波动五项指标，结果存入emotions表。" + NL + NL
    "第五阶段，市场验证。市场验证器将情绪指标与行情数据进行对齐，通过Pearson/Spearman相关分析、方向预测准确率和格兰杰因果检验评估情绪指标的有效性。" + NL + NL
    "第六阶段，可视化展示。前端通过API获取各阶段的处理结果，以图表和表格的形式直观展示给用户。"
)
new = (
    "系统的核心数据流遵循"\"采集→处理→分析→量化→验证→展示\""的六阶段流水线模式连贯运转。在数据采集阶段，爬虫服务从东方财富股吧批量采集投资者评论，解析HTML页面提取评论内容、作者和发布时间等信息后存入comments表。在文本处理阶段，文本处理器对原始评论依次进行清洗（去除HTML标签、URL、表情等噪声）、分词（基于jieba的金融定制化分词）和去重（MD5哈希精确去重加Jaccard模糊去重），清洗后的干净文本写入content_clean字段供后续使用。在情感分析阶段，BERT模型对清洗后文本进行三分类推理，输出情感标签（积极/中性/消极）和置信度，结果存入sentiments表；同时方面级分析器对文本进行业绩、政策、技术面、资金流向、行业趋势五个维度的情感判断，结果存入aspect_sentiments表。在情绪量化阶段，情绪计算器按股票和时间粒度（日级或小时级）聚合所有评论的情感分析结果，计算五项量化指标后存入emotions表。在市场验证阶段，市场验证器将情绪指标与行情数据进行时间对齐，通过相关分析、方向预测和格兰杰因果检验等统计方法评估情绪指标的有效性。在可视化展示阶段，前端通过API获取各阶段处理结果，以图表和表格的形式直观呈现给用户。"
)
do_replace(old, new, "4.1.2 核心数据流")

# ===== 4.2.1 模型架构 =====
old = (
    "情感分析模型（SentimentAnalyzer）是系统的核心AI组件，基于Chinese-BERT-wwm-ext预训练模型构建。模型架构包含以下层次：" + NL + NL
    "（1）输入层。接收经过文本处理器清洗后的评论文本，通过AutoTokenizer将文本转换为Token ID序列，最大输入长度为128个Token。对于超长文本进行截断处理，短文本进行Padding填充。" + NL + NL
    "（2）BERT编码层。包含12层Transformer编码器，768维隐藏层，12个注意力头。输入经过Token Embedding、Segment Embedding和Position Embedding三者相加后，逐层通过多头自注意力和前馈神经网络进行特征提取。" + NL + NL
    "（3）分类输出层。取BERT最后一层[CLS]标记对应的768维隐藏向量，通过线性变换映射为3维输出（对应积极、中性、消极三个类别），再经过Softmax函数转换为概率分布。" + NL + NL
    "模型配置通过SentimentConfig数据类管理，核心参数包括：模型名称（hfl/chinese-bert-wwm-ext）、最大序列长度（128）、分类数（3）和标签映射（0:negative, 1:neutral, 2:positive）。系统自动检测CUDA设备，在GPU可用时利用GPU加速推理。"
)
new = (
    "情感分析模型（SentimentAnalyzer）是系统的核心AI组件，基于Chinese-BERT-wwm-ext预训练模型构建，架构上分为三个层次。输入层负责接收经过文本处理器清洗后的评论文本，通过AutoTokenizer将文本转换为Token ID序列，最大输入长度设置为128个Token，超长文本做截断处理，短文本做Padding填充。BERT编码层包含12层Transformer编码器、768维隐藏层和12个注意力头，输入经过Token Embedding、Segment Embedding和Position Embedding三者相加后，逐层通过多头自注意力和前馈神经网络完成深层语义特征提取。分类输出层取BERT最后一层[CLS]标记对应的768维隐藏向量，通过线性变换映射为3维输出（对应积极、中性、消极三个类别），再经过Softmax函数转换为概率分布。模型配置通过SentimentConfig数据类统一管理，核心参数包括模型名称（hfl/chinese-bert-wwm-ext）、最大序列长度（128）、分类数（3）和标签映射（0:negative, 1:neutral, 2:positive），系统还会自动检测CUDA设备，在GPU可用时利用GPU加速推理。"
)
do_replace(old, new, "4.2.1 模型架构")

# ===== 4.2.2 模型加载策略 =====
old = (
    "为适应不同的部署环境，系统采用多路径模型加载策略：" + NL + NL
    "首先，按优先级依次检查四个本地模型路径（配置目录、相对路径、项目路径等），查找包含config.json的模型目录。若找到本地微调模型，使用`local_files_only=True`参数加载，无需网络连接。若本地模型不存在，则通过Hugging Face Hub下载预训练模型，并在下载后缓存到本地以供后续使用。若ML库（torch、transformers）未安装，系统自动降级为规则模式，基于关键词匹配进行简单的情感判断，确保系统在任何环境下都能正常运行。"
)
new = (
    "为适应不同的部署环境，系统采用多路径模型加载策略，按优先级依次检查本地模型路径（配置目录、相对路径、项目路径等），查找包含config.json的模型目录。若找到本地微调模型则使用`local_files_only=True`参数直接加载，无需网络连接；若本地模型不存在则通过Hugging Face Hub下载预训练模型并在本地缓存；若torch或transformers等ML库未安装，系统自动降级为基于关键词匹配的规则模式进行简单情感判断，确保在任何环境下都能正常运行。"
)
do_replace(old, new, "4.2.2 模型加载")

# ===== 4.3.2 词典分析算法 =====
old = (
    "词典分析算法采用顺序扫描策略，维护当前的修饰状态（程度倍数和否定标记），逐词处理分词后的文本序列：" + NL + NL
    "（1）遇到程度词时，更新当前程度倍数。" + NL + NL
    "（2）遇到否定词时，翻转当前否定标记。" + NL + NL
    "（3）遇到情感词时，取其基础权重乘以当前程度倍数；若否定标记为真，则反转极性。将修饰后的权重累加到总分。" + NL + NL
    "（4）每处理完一个情感词后，重置程度倍数为1.0、否定标记为false。" + NL + NL
    "最终输出包括综合情感分数、积极词数量和列表、消极词数量和列表，为用户提供透明的分析依据。"
)
new = (
    "词典分析算法采用顺序扫描策略，维护当前的修饰状态（程度倍数和否定标记），逐词遍历分词后的文本序列。当遇到程度词时更新当前程度倍数以放大或缩小后续情感词的权重；遇到否定词时翻转否定标记以反转后续情感词的极性；遇到情感词时，取其基础权重乘以当前程度倍数，若否定标记为真则反转极性，然后将修饰后的权重累加到总分；每处理完一个情感词后重置程度倍数为1.0、否定标记为false，为处理下一个情感词做准备。最终输出综合情感分数、积极词和消极词的数量与列表，为用户提供透明的分析依据。"
)
do_replace(old, new, "4.3.2 词典算法")

# ===== 4.5.1 相关性分析 =====
old = (
    "市场验证器（MarketValidator）实现了情绪指标与市场走势之间的统计验证。相关性分析的核心流程为：" + NL + NL
    "（1）数据获取。从emotions表和quotes表中分别获取指定股票在指定时间范围内的情绪强度序列和涨跌幅序列。" + NL + NL
    "（2）数据对齐。考虑到情绪可能领先于市场反应，系统支持设置lag_days参数（0-5天）。当lag_days=1时，将T日的情绪强度与T+1日的涨跌幅对齐，检验情绪对次日走势的预测能力。" + NL + NL
    "（3）相关性计算。使用SciPy库的`pearsonr`和`spearmanr`函数分别计算Pearson线性相关系数和Spearman秩相关系数，同时返回p值用于显著性检验。" + NL + NL
    "（4）显著性判定。当p<0.05时认为相关性具有统计显著性。将相关系数绝对值按阈值划分为高（≥0.7）、中等（≥0.4）和低（<0.4）三个等级。"
)
new = (
    "市场验证器（MarketValidator）实现了情绪指标与市场走势之间的统计验证。分析流程依次包括：数据获取阶段从emotions表和quotes表中分别提取指定股票在指定时间范围内的情绪强度序列和涨跌幅序列；数据对齐阶段考虑到情绪可能领先于市场反应，系统支持设置lag_days参数（0-5天），例如lag_days=1时将T日的情绪强度与T+1日的涨跌幅对齐，以检验情绪对次日走势的预测能力；相关性计算阶段使用SciPy库的`pearsonr`和`spearmanr`函数分别计算Pearson线性相关系数和Spearman秩相关系数，同时返回p值用于显著性检验；显著性判定阶段当p<0.05时认为相关性具有统计显著性，并将相关系数绝对值按阈值划分为高（≥0.7）、中等（≥0.4）和低（<0.4）三个等级以便结果解读。"
)
do_replace(old, new, "4.5.1 相关性分析")

# ===== 4.5.2 方向预测准确率 =====
old = (
    "方向预测准确率验证了情绪方向（看涨/看跌）与实际股价涨跌方向的一致性：" + NL + NL
    "（1）将情绪强度>0记为看涨方向（+1），情绪强度<0记为看跌方向（-1），情绪强度=0的数据点不参与统计。" + NL + NL
    "（2）将对应交易日（考虑lag）的涨跌幅>0记为上涨方向（+1），<0记为下跌方向（-1）。" + NL + NL
    "（3）计算方向一致的天数占有效总天数的比例作为准确率。准确率超过50%说明情绪指标具有一定的方向预测能力。"
)
new = (
    "方向预测准确率验证了情绪方向与实际股价涨跌方向之间的一致性。具体的做法是：将情绪强度大于0记为看涨方向（+1），小于0记为看跌方向（-1），等于0的数据点因无明确倾向而不参与统计；将对应交易日（考虑lag后）的涨跌幅大于0记为上涨方向（+1），小于0记为下跌方向（-1）；最后统计方向一致的天数占有效总天数的比例作为准确率指标。准确率超过50%的随机基线则说明情绪指标具有一定的方向预测能力。"
)
do_replace(old, new, "4.5.2 方向预测")

# ===== 4.5.3 最优领先天数搜索 =====
old = (
    "系统通过遍历不同的lag_days（0到5天），分别计算各滞后期的相关系数，选择相关系数绝对值最大的lag作为最优领先天数。最优领先天数反映了情绪信号到市场反应之间的时间延迟。"
)
new = (
    "系统通过遍历不同的lag_days参数（0到5天），分别计算各滞后期的相关系数，选择相关系数绝对值最大的lag作为最优领先天数。这一指标反映了从情绪信号出现到市场做出反应之间的时间延迟，帮助判断情绪因子的最佳前置天数。"
)
do_replace(old, new, "4.5.3 最优领先天数")

# ===== 5.1.3 测试数据集 =====
old = (
    "系统测试使用两类数据集：" + NL + NL
    "（1）情感分析测试集。从采集的8万条东方财富股评中，随机抽取2000条进行人工标注（积极/中性/消极），作为情感分析模型的评估基准。标注由3名金融专业背景的人员完成，对于有争议的样本采用多数投票法确定最终标签。标注后的数据按8:1:1的比例划分为训练集（1600条）、验证集（200条）和测试集（200条）。" + NL + NL
    "（2）市场验证测试集。选取沪深300成分股中交易活跃、评论量充足的30只股票，使用2023年全年的情绪数据和行情数据进行相关性验证。"
)
new = (
    "系统测试使用两类数据集。情感分析测试集从采集的8万条东方财富股评中随机抽取2000条，由3名具有金融专业背景的人员独立进行标注（积极/中性/消极），对有争议的样本采用多数投票法确定最终标签，标注后的数据按8:1:1的比例划分为训练集（1600条）、验证集（200条）和测试集（200条）。市场验证测试集则选取沪深300成分股中交易活跃、评论量充足的30只股票，使用2023年全年的情绪数据和行情数据进行相关性验证。"
)
do_replace(old, new, "5.1.3 测试数据集")

# Print results
print("\n=== Replacement Results ===")
for r in results:
    print(r)

ok_count = len([x for x in results if x.startswith("OK")])
print(f"\nTotal: {ok_count}/{len(results)} replaced, {len(results) - ok_count} not found")

with open('初稿.md', 'w', encoding='utf-8') as f:
    f.write(content)
print("File saved.")
